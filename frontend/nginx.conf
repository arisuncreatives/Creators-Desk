server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        
        # This line is the magic fix for React Router
        try_files $uri $uri/ /index.html; 
    }
}